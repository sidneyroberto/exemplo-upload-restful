# exemplo-upload-restful

Exemplo de serviço RESTful de upload de arquivos com Node, Express e TypeScript.

Para executar o projeto, primeiro instale as dependências:

#### `npm i`

Após, execute o projeto:

#### `npm run dev`